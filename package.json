{
  "name": "web-worker-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.1.0",
    "express": "^4.17.1",
    "mongoose": "^5.6.11",
    "path": "^0.12.7",
    "web-push": "^3.3.5"
  },
  "devDependencies": {
    "@types/cors": "^2.8.6",
    "@types/dotenv": "^6.1.1",
    "@types/express": "^4.17.1",
    "@types/mongoose": "^5.5.16"
  },
  "scripts": {
    "dev": "yarn run build:live",
    "start": "node public/index.js",
    "tsc": "tsc",
    "build": "rm -rf dist && yarn run tsc -p . --outDir ./public",
    "build:live": "nodemon --watch '**/**/*.ts' --exec 'ts-node' src/index.ts"
  }
}
